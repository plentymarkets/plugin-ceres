{% extends getPartial('page-design') %}

{% import "Ceres::PageDesign.Macros.LayoutContainer" as LayoutContainer %}

{% block PartialHead %}
    {% embed getPartial('page-metadata') %}
        {% block title          trans("Ceres::Template.privacyPolicy", {"hyphen": ""}) %}
        {% block description    trans('Ceres::Template.privacyPolicyMetaDescription') %}
        {% block robots         ceresConfig.meta.robotsPrivacyPolicy %}
    {% endembed %}
{% endblock %}

{% block PageBody %}

    {% set declarationOfAccessibility = services.legalInformation.getDeclarationOfAccessibility() %}

    <div class="container-max page-content">
        <div class="row">
            <div class="col-12">
                {% autoescape false %}
                    <h1 class="pt-3">{{ trans("Ceres::Template.privacyPolicy", {"hyphen": "&shy;"}) }}</h1>
                {% endautoescape %}

                <hr class="my-5">

                {% if privacyPolicy.htmlText %}
                    {{ declarationOfAccessibility.htmlText | raw }}
                {% else %}
                    <p>{{ declarationOfAccessibility.plainText }}</p>
                {% endif %}

                {{ LayoutContainer.show("Ceres::StaticPages.DeclarationOfAccessibility") }}
            </div>
        </div>
    </div>

{% endblock %}
