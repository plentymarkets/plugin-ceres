{% macro getInlineSpacings( spacingValue, spacingType ) %}
    {%- spaceless %}
        {% for direction in ["top", "right", "bottom", "left"] if spacingValue[direction].unit is defined and spacingValue[direction].unit.mobile is not null %}
            {{ spacingType | default("margin") }}-{{ direction }}: {{ spacingValue[direction].value.mobile }}{{ spacingValue[direction].unit.mobile }}
        {% endfor %}
    {% endspaceless -%}
{% endmacro %}

{% macro getSpacingClasses( spacingValue, classPrefix ) %}
    {%- spaceless %}
        {% for d, direction in {"t": "top", "r": "right", "b": "bottom", "l": "left"} if spacingValue[direction].unit is defined and spacingValue[direction].unit.mobile is null and spacingValue[direction].value.mobile > 0 %}
            {{ spacingType | default("m") }}{{ d }}-{{ spacingValue[direction].value.mobile }}
        {% endfor %}
    {% endspaceless -%}
{% endmacro %}