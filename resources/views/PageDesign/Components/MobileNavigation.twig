<script type="x/template" id="vue-mobile-navigation">
    <div class="mobile-navigation">

        <ul class="breadcrumb">
            <li class="btn-close" @click="closeNavigation()"></li>

            <li class="breadcrumb-item" @click="slideTo(categoryTree, true)">
                <i class="fa fa-home" aria-hidden="true"></i>
            </li>

            <li  class="breadcrumb-item" v-for="breadcrumb in breadcrumbs" @click="slideTo(breadcrumb.layer, true)">
                ${ breadcrumb.name }
            </li>
        </ul>


        <ul v-menu id="menu-1" class="mainmenu menu-active">
            <li class="ddown" v-for="category in dataContainer1" v-show="!category.hideCategory">
                <a :href="category.url" @click.prevent="navigateTo(category)" v-if="category.showChilds || category.details[0]">${ category.details[0].name }</a>

                <span class="nav-direction" v-if="category.children && category.showChilds" @click="slideTo(category.children)">
                    <i class="fa fa-caret-right" aria-hidden="true"></i>
                </span>
            </li>
        </ul>

        <ul v-menu id="menu-2" class="mainmenu">
            <li class="ddown" v-for="category in dataContainer2" v-show="!category.hideCategory">
                <a :href="category.url" @click.prevent="navigateTo(category)" v-if="category.showChilds || category.details[0]">${ category.details[0].name }</a>
                
                <span class="nav-direction" v-if="category.children && category.showChilds" @click="slideTo(category.children)">
                    <i class="fa fa-caret-right" aria-hidden="true"></i>
                </span>
            </li>
        </ul>

    </div>
</script>