<b>{{ trans("Ceres::Template.contactSenderMail") }}:</b> {{ userMail }}<br>

{% if orderId | length > 0 %}
    <b>{{ trans("Ceres::Template.contactOrderId") }}:</b> {{ orderId }}<br><br>
{% endif %}

{{ message }}
_________________________________________________________

{{ trans("Ceres::Template.contactBestRegards") }}, 
{{ name }}