<script type="x/template" id="vue-change-email-form">
    <form>
        <div class="container-max page-content">
            <div class="row">
                <div class="col-12 col-sm-8 col-lg-6 mb-5 content-center">
                    <h1 class="mb-5 mt-4">{{ trans("Ceres::Template.myAccountChangeEmail") }}</h1>

                    <span>{{ trans("Ceres::Template.myAccountChangeEmailInfoText") }}</span>

                    <div class="input-unit mt-3">
                        <label for="email-change-email">{{ trans("Ceres::Template.myAccountNewEmail") }}</label>
                        <input type="email" name="email" autocomplete="email" class="form-control" id="email-change-email" v-model="email">
                    </div>

                    <div class="input-unit">
                        <label for="email-change-password">{{ trans("Ceres::Template.loginPassword") }}</label>
                        <input type="password" name="password" autocomplete="current-password" class="form-control" id="email-change-password" v-model="password">
                    </div>

                    <hr>

                    <span class="input-group-btn">
                        <button @click="sendLogin" :disabled="isDisabled" class="btn btn-primary float-right btn-medium btn-xs-max-width">
                            <i class="fa fa-envelope" v-waiting-animation="isDisabled" aria-hidden="true"></i>
                            <span>{{ trans("Ceres::Template.myAccountChangeEmail") }}</span>
                        </button>
                    </span>
                </div>
            </div>
        </div>
    </form>
</script>