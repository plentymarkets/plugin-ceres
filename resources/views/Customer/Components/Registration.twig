{{ component( "Ceres::Customer.Components.AddressInputGroup.AddressInputGroup" ) }}
<script type="x/template" id="vue-registration">
    <div>
        <div class="modal-body">
            <div class="row" :id="'registration' + _uid">

                <div class="col-sm-12">
                    <div class="alert alert-info password-info">
                        <span class="info-badge">{{ trans("Ceres::Template.accPasswordReq") }}</span>
                    </div>
                </div>

                <div class="col-sm-12">
                    <div class="input-unit" data-validate="mail">
                        <input type="text" name="email" id="txt_email" v-model="username">
                        <label for="txt_email">{{ trans("Ceres::Template.accEmail") }}*</label>
                    </div>
                </div>

                <div class="col-sm-6" v-if="!guestMode">
                    <div class="input-unit" :class="{'no-bottom media-xs-d': modalElement}" id="passwordBox" data-validate="password">
                        <input type="password" name="password" id="registration_password_input" v-model="password">
                        <label for="txt_pw">{{ trans("Ceres::Template.accPassword") }}*</label>
                    </div>
                </div>

                <div class="col-sm-6" v-if="!guestMode">
                    <div class="input-unit" :class="{'no-bottom': modalElement}" id="passwordRepeatBox" data-validate="ref">
                        <input type="password" name="password-repeat" id="txt_pwrepeat" v-model="passwordRepeat" data-validate-ref="#registration_password_input">
                        <label for="txt_pwrepeat">{{ trans("Ceres::Template.accRepeatPassword") }}*</label>
                    </div>
                </div>

                <address-input-group
                    template="#vue-address-input-group"
                    v-if="!isSimpleRegistration"
                    address-type="1"
                    :value="billingAddress"
                    @input="setAddressDataField($event)">
                </address-input-group>
            </div>
        </div>
        <div class="modal-footer">
            {{ LayoutContainer.show("Ceres::RegistrationOverlay.ExtendOverlayButtons") }}
            <a style="margin-top:5px; float:left" data-toggle="collapse" href="#register-information" role="button" aria-expanded="false" aria-controls="collapse">Kundenkonto Informationen</a>
            <button @click="validateRegistration" :disabled="isDisabled" class="btn btn-primary btn-medium btn-right">
                <i class="fa fa-user-plus" v-waiting-animation="isDisabled" aria-hidden="true"></i> 
                {{ trans("Ceres::Template.accRegister") }}
            </button>
            <div class="row float-left">
                <div class="col-xs-12">
                    <div class="collapse multi-collapse" id="register-information">
                        <!-- <div class="card card-body" style="margin-top:20px;"> -->
                            <div class="alert alert-success" style="margin-top:20px;text-align:left;">
                                <ul class="p-l-1">
                                    <li class="m-b-1">Wir bieten Ihnen die Speicherung Ihrer persönlichen Daten in einem passwortgeschützten Kundenkonto an, sodass Sie bei Ihrem nächsten Einkauf nicht erneut Ihren Namen und Ihre Anschrift eingeben müssen.</li>
                                    <li class="m-b-1">Wir bieten Ihnen die Speicherung Ihrer persönlichen Daten in einem passwortgeschützten Kundenkonto an, sodass Sie bei Ihrem nächsten Einkauf nicht erneut Ihren Namen und Ihre Anschrift eingeben müssen.</li>
                                </ul>
                            </div>
                        <!-- </div> -->
                    </div>
                </div>
            </div>
        </div>
    </div>
</script>
