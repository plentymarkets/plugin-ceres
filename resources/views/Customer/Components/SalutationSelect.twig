<script type="x/template" id="vue-salutation-select" >
    <select :value="addressData.addressSalutation" class="custom-select" @input="emitInputEvent(parseInt($event.target.value))">
        <option :value="salutation.id" :selected="addressData.addressSalutation === index" v-for="(salutation, index) in currentSalutation">
            ${ salutation.value }
        </option>
    </select>
</script>
