{% extends getPartial('page-design') %}

{% block PartialHead %}
    <meta name="robots" content="NOINDEX, NOFOLLOW">
    <title>{{ trans("Ceres::Template.resetPwResetPassword") }} | {{ trans("Ceres::Template.headerCompanyName") }}</title>
{% endblock %}

{% block PageBody %}
    {{ component( "Ceres::Customer.Components.ResetPasswordForm") }}

    <div class="container-max reset-pwd m-b-3 m-t-1">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-8 offset-md-2 col-lg-6 offset-lg-3 m-b-2">
                <h1 class="h2">{{ trans("Ceres::Template.resetPwResetPassword") }}</h1>
            </div>
        </div>

        <reset-password-form template="#vue-reset-password-form" :contact-id="{{ contactId }}" hash="{{ hash }}"></reset-password-form>
    </div>
{% endblock %}