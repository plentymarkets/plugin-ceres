{% import "Ceres::PageDesign.Macros.LayoutContainer" as LayoutContainer %}

<script type="x/template" id="vue-place-order">
    <div>
        {% set buttonReplacement = LayoutContainer.show("Ceres::Checkout.PlaceOrder") %}
        {% if buttonReplacement|trim is not empty %}
            {{ buttonReplacement }}
        {% else %}
            <button
                    class="btn btn-block btn-primary btn-appearance"
                    :class="buttonClasses"
                    :style="paddingInlineStyles"
                    @click="placeOrder()"
                    :disabled="waiting || isBasketLoading || !isBasketInitiallyLoaded || isInvalidShippingCountry"
                    {{ set_testing_attr("data-testing", "place-order") }}>
                <icon icon="arrow-right" :loading="waiting"></icon>
                {{ trans("Ceres::Template.checkoutBuyNow") }}
            </button>
        {% endif %}

        {% if ceresConfig.checkout.showGuaranteeNotice %}
            <div class="text-center mt-2">
                <a
                        href="{{ plugin_path('Ceres') }}/images/guarantee_notice/{{ guarantee_notice_svg_filename(lang) }}"
                        data-lightbox="guarantee-notice"
                        data-title="{{ trans("Ceres::Template.checkoutGuaranteeNoticeLink") }}">
                    {{ trans("Ceres::Template.checkoutGuaranteeNoticeLink") }}
                </a>
            </div>
        {% endif %}
    </div>
</script>
