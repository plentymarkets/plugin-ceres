{% import "Ceres::PageDesign.Macros.LayoutContainer" as LayoutContainer %}

<script type="x/template" id="vue-place-order">
    <div>
        {% set buttonReplacement = LayoutContainer.show("Ceres::Checkout.PlaceOrder") %}
        {% if buttonReplacement|trim is not empty %}
            {{ buttonReplacement }}
        {% else %}


                <button
                    v-tooltip="isInvalidShippingCountry"
                    data-placement="top"
                    data-toggle="tooltip"
                    title="{{ trans('Ceres::Template.checkoutBuyNowTooltip') }}"
                    class="btn btn-block btn-primary btn-appearance "
                    :class="[isInvalidShippingCountry ? 'disabled' : '', buttonClasses]"
                    :style="paddingInlineStyles"
                    @click="placeOrder()"
                    :disabled="waiting || isBasketLoading || !isBasketInitiallyLoaded"
                    {{ set_testing_attr("data-testing", "place-order") }}>
                    <icon icon="arrow-right" :loading="waiting"></icon>
                    {{ trans("Ceres::Template.checkoutBuyNow") }}
                </button>


        {% endif %}
    </div>
</script>
