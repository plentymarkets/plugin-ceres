{% set decimalCount = config("PluginCeres.item.storeSpecial") %}

<script type="x/template" id="vue-item-list">
    <hr class="m-b-1 hidden-sm-down">
    <ul class="product-list row grid" v-show="!itemSearch.isLoading">
        <category-item v-for="itemData in testData.rows" :item-data="itemData.data"
                       decimal-count="{{ decimalCount }}"></category-item>
    </ul>
    <hr class="m-y-2">
</script>