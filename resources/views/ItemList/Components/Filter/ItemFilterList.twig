<script type="x/template" id="vue-item-filter-list">
    <div class="filter-wrapper" v-show="facets && facets.length > 0">
        {{ component( "Ceres::ItemList.Components.Filter.ItemFilter") }}
        {{ component( "Ceres::ItemList.Components.Filter.ItemFilterPrice") }}

        <a class="btn btn-link filter-toggle" data-toggle="collapse" href="#filterCollapse" aria-controls="filterCollapse" @click="toggleOpeningState()">
            <i class="fa fa-sliders default-float" aria-hidden="true"></i> {{ trans("Ceres::Template.itemFilter") }}
        </a>

        <div class="collapse" :class="{in: isActive}" id="filterCollapse">
            <div class="container-max page-content component-loading with-icon refreshing" :class="{ 'isLoading': isLoading }">
                <div class="card-columns">
                    <item-filter v-for="facet in facets" :facet="facet" :key="facet.id"></item-filter>
                </div>

                <div class="row">
                    <div class="col-xs-6">
                        <button type="button" class="btn btn-success" data-toggle="collapse" href="#filterCollapse" aria-controls="filterCollapse" @click="$store.dispatch('loadItemList')">
                            <i aria-hidden="true">🦄</i> Filter anwenden <i aria-hidden="true">🦄</i>
                        </button>
                    </div>
                    <div class="col-xs-6 text-xs-right">
                        <button type="button" class="btn btn-primary btn-medium" data-toggle="collapse" href="#filterCollapse" aria-controls="filterCollapse" @click="toggleOpeningState()">
                            <i class="fa fa-times" aria-hidden="true"></i> {{ trans("Ceres::Template.itemClose") }}
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</script>
