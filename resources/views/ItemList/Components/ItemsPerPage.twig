<script type="x/template" id="vue-items-per-page">
    <div class="input-unit input-unit-no-border">
        <select class="custom-select" name="items_per_page" id="listItemsPerPage" v-model="itemSearch.itemsPerPage"
                @change="itemsPerPageChanged()">
            <option :value="element" v-for="element in paginationValues">
                ${ element }
            </option>
        </select>
        <label for="listItemsPerPage">{{ trans("PluginCeres::Template.itemCategoryItemsPerPage") }}</label>
    </div>
</script>