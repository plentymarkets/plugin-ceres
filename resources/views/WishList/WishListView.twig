{% extends getPartial('page-design') %}

{% block PartialHead %}
    <title>Wish List</title>
{% endblock %}

{% block PageBody %}
    {{ component( "Ceres::WishList.Components.WishList" ) }}
    <!-- {{ wishList | json_encode() }} -->
    <div class="page-content basket container-max">

        <div class="row m-t-1">
            <div class="col-xs-12">
                {# <h1 class="h4">{{ trans("Ceres::Template.basket") }}</h1> #}
                <h1 class="h4">WISHLIST</h1>
            </div>
        </div>

        <hr>

         <wish-list template="#vue-wish-list" :wish-list-ids="{{ wishList | json_encode() }}"></wish-list>
    </div>
{% endblock %}
