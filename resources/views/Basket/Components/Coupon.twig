<script type="x/template" id="vue-coupon">
    <div class="cmp">
        <div class="input-group">
            <input type="text" class="form-control" v-model="couponCode" placeholder="{{ trans("Ceres::Template.couponEnterCoupon") }}" @keyup.enter="redeemCode()" :disabled="disabled">
            <span class="input-group-btn">
                <button class="btn btn-medium" :class="'btn-' + appearance" type="button" @click="redeemCode()" :disabled="waiting" v-if="!disabled">
                    <i v-waiting-animation="waiting" class="fa fa-gift" aria-hidden="true"></i>
                    {{ trans("Ceres::Template.couponRedeem") }}
                </button>
                <button class="btn btn-medium btn-danger" type="button" @click="removeCode()" :disabled="waiting" v-else>
                    <i v-waiting-animation="waiting" class="fa fa-trash" aria-hidden="true"></i>
                    {{ trans("Ceres::Template.couponRemove") }}
                </button>
            </span>
        </div>
    </div>
</script>
