<script type="x/template" id="vue-variation-image-list">
    <div>
        <div class="owl-carousel owl-carousel-single" v-el:single>
            <div class="owl_item" v-for="image in currentVariation.documents[0].data.images.all">
                <div class="square-container">
                    <div class="square-inner center-xy main-image">
                        <a :href="image.url" data-lightbox="single-big-image-gallery">
                            <img :src="image.url" class="img-fluid">
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="owl-carousel owl-carousel-preview" v-el:preview>
            <div class="owl_item" v-for="image in currentVariation.documents[0].data.images.all" :class="{ 'synced': $index == currentItem }" @click="goTo( $index )">
                <img :src="image.urlPreview" class="img-fluid">
            </div>
        </div>
    </div>
</script>
