<script type="x/template" id="vue-basket-list">
    <basket-list-item  v-for="basketItem in basketItems" :basket-item="basketItem" :size="size"></basket-list-item>

    <div v-if="!basketItems.length > 0">
        <h5>{{ trans("PluginCallisto::Callisto.orderNoItemsInBasket") }}</h5>
    </div>
</script>